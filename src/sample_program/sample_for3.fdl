def int main()
   	path f
    path dir
    path dest
    int count

    dir = "."
    dest = "./test"
    count = 0

	for ( f in dir ) 
        	if (f.type == ".sh") then
                dest <- f
            end
		print f
	end
	return 1
end
