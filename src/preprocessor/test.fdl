def list listAllFiles(path dirPath, int depth)
    int currentDepth
    list myList
    path myPath
    dict myDict
    bool myBool
    string myString
    bool something

    for item in myList
        print("Something")
    end

    if (something)
        for item in myList
            print("Something")
        end
    end

    if (something)
        print("Something")
    else
        print("Something")
    end

    while (something)
        print("Something")
    end
end